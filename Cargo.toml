[package]
name = "board_editor"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.8.0", features = ["dynamic"] }
regex = "1.6.0"

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 2

[profile.dev.package."*"]
opt-level = 3